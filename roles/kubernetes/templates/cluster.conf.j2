apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
kubernetesVersion: {{ master.cluster.kubernetes_version }}
controlPlaneEndpoint: "{{ naster.cluster.control_plane_endpoint }}"
networking:
    podSubnet: "{{ master.cluster.pod_subnet }}"